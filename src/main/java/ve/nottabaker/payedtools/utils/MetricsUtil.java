package ve.nottabaker.payedtools.utils;

import ve.nottabaker.payedtools.PayEdtools;

/**
 * Placeholder utility for bStats metrics
 * In production, this would integrate with bStats
 */
public class MetricsUtil {
    
    public static void initialize(PayEdtools plugin) {
        // In production, initialize bStats here
        Logger.debug("Metrics initialized (placeholder)");
    }
}
